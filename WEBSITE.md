# YouHooAlert Website

The **marketing / landing website** lives in:

```
website/
```

It is a **static Next.js site** with no API calls. The Expo mobile app in this repo root is unchanged.

## Quick start

```bash
cd website
npm install
npm run dev
```

Set `SITE_URL` in `website/lib/site.ts` before production deploy.
